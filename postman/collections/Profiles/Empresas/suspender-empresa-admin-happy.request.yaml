$kind: http-request
name: Suspender Empresa por Admin (Happy)
method: PATCH
url: '{{baseUrl}}/profiles/empresas/{{empresaId}}/cambiar_status/'
order: 7120
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "status": "suspendida"
    }
scripts:
  - type: beforeRequest
    language: text/javascript
    code: |-
      pm.environment.set("accessToken", pm.environment.get("adminAccessToken"));
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200 - Admin suspende la empresa", function () {
          pm.response.to.have.status(200);
      });
      pm.test("Status de empresa actualizado a suspendida", function () {
          var json = pm.response.json();
          pm.expect(json.status).to.eql("suspendida");
      });
