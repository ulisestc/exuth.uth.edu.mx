$kind: http-request
name: Get Giro - No Existe (Sad)
method: GET
url: '{{baseUrl}}/core/giros/99999/'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 404", () => pm.response.to.have.status(404));
