$kind: http-request
name: List Carreras - Sin Token (Sad)
method: GET
url: '{{baseUrl}}/core/carreras/'
order: 2000
auth:
  type: noauth
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 401", () => pm.response.to.have.status(401));
