$kind: http-request
name: Cambiar Status Vacante por Admin (Happy)
method: PATCH
url: '{{baseUrl}}/vacantes/{{vacanteId}}/cambiar_status/'
order: 7200
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "status": "aprobada"
    }
scripts:
  - type: beforeRequest
    language: text/javascript
    code: |-
      pm.environment.set("accessToken", pm.environment.get("adminAccessToken"));
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200 - Admin aprueba la vacante", function () {
          pm.response.to.have.status(200);
      });
      pm.test("Estatus de la vacante actualizado a aprobada", function () {
          var json = pm.response.json();
          pm.expect(json.status).to.eql("aprobada");
      });
