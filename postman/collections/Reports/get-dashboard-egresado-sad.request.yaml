$kind: http-request
name: Get Dashboard Stats por Egresado (Sad - RBAC)
method: GET
url: '{{baseUrl}}/reports/dashboard/'
order: 1100
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
scripts:
  - type: beforeRequest
    language: text/javascript
    code: |-
      pm.environment.set("accessToken", pm.environment.get("egresadoAccessToken"));
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 403 - Egresado no tiene acceso a reportes", function () {
          pm.response.to.have.status(403);
      });
